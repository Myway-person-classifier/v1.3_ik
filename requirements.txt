# Colab GPU 환경용 requirements.txt
# Colab에 기본 설치된 패키지는 제외하고 실제로 필요한 패키지만 포함
# Colab에 기본 설치: numpy, pandas, matplotlib, seaborn, scikit-learn, scipy, torch, torchvision, tqdm, requests 등

# ===== Hugging Face & Transformers =====
# transformers는 Colab에 설치되어 있지만 버전이 다를 수 있으므로 명시
transformers==4.53.2
datasets==4.0.0
huggingface-hub==0.33.2
tokenizers==0.21.4
sentencepiece==0.2.1
safetensors==0.5.3

# ===== Training & Optimization =====
accelerate==1.12.0
peft==0.18.1
bitsandbytes==0.46.1
torchmetrics==1.7.3

# ===== Data Processing =====
# numpy, pandas는 Colab에 기본 설치되어 있지만 버전 호환을 위해 명시 가능
# numpy==2.0.2  # 필요시 주석 해제
# pandas==2.3.0  # 필요시 주석 해제
pyarrow==20.0.0
fsspec==2025.3.0

# ===== Utilities =====
# scikit-learn, scipy는 Colab에 기본 설치
# scikit-learn==1.7.0  # 필요시 주석 해제
# scipy==1.15.3  # 필요시 주석 해제
joblib==1.5.1
GitPython==3.1.46

# ===== Optional / Logging =====
wandb==0.21.0  # 선택적: 사용하지 않으면 주석 처리

# ===== Dependencies =====
# HTTP & Networking
aiohttp==3.13.3
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
async-timeout==5.0.1
multidict==6.7.0
frozenlist==1.8.0
yarl==1.22.0

# Data validation & parsing
pydantic==2.11.10
pydantic_core==2.33.2
PyYAML==6.0.3

# System & File
filelock==3.20.3
psutil==7.2.1
dill==0.3.8
multiprocess==0.70.16

# Other dependencies
annotated-types==0.7.0
attrs==25.4.0
lightning-utilities==0.15.2
networkx==3.4.2
packaging==25.0
platformdirs==4.5.1
propcache==0.4.1
protobuf==6.31.1
python-dateutil==2.9.0.post0
pytz==2025.2
regex==2025.11.3
sentry-sdk==2.49.0
six==1.17.0
smmap==5.0.2
sympy==1.14.0
threadpoolctl==3.6.0
typing-inspection==0.4.2
typing_extensions==4.14.0
tzdata==2025.3
xxhash==3.6.0

# ===== Note =====
# Colab에 기본 설치되어 있어 제외한 패키지:
# - torch, torchvision (Colab에 설치되어 있음, 버전은 다를 수 있음)
# - numpy, pandas (Colab에 설치되어 있음)
# - matplotlib, seaborn (Colab에 설치되어 있음)
# - scikit-learn, scipy (Colab에 설치되어 있음)
# - tqdm (Colab에 설치되어 있음)
# - requests, certifi, urllib3 (Colab에 설치되어 있음)
# - pillow (PIL) (Colab에 설치되어 있음)
# - jinja2, markupsafe (Colab에 설치되어 있음)
# - idna, charset-normalizer (Colab에 설치되어 있음)
# - click, colorama (Colab에 설치되어 있음)
# - mpmath (Colab에 설치되어 있음)
